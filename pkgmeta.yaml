package-as: Neuron

enable-nolib-creation: no
enable-toc-creation: yes

optional-dependencies:
  - masque
  - masque-neuron

embedded-libraries:
  - Ace3
  - LibDualSpec-1-0
  - LibDBIcon-1-0
  - LibDeflate

# theoretically we can get rid of the ignore files now that we have the makefile
ignore:
  - README.md
  - dist
